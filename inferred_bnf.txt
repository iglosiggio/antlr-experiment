<file> ::= <report-header> <parameter-list> <data-list> <statement-list> EOF

<report-header>  ::= "REPORT" <identifier> "."
<parameter-list> ::= <parameter-list> <parameter> | <parameter>
<data-list>      ::= <data-list> <data> | <data>
<statement-list> ::= <statement-list> <statement> | <statement>

<parameter> ::= "PARAMETER" <identifier> "TYPE" <type> "."
<data>      ::= "DATA" <identifier> "TYPE" <type> "."
<statement> ::= <assignment-statement> | <write-statement> | <cocatenate-statement>

<assignment-statement>  ::= <identifier> "=" <expression> "."
<write-statement>       ::= "WRITE" <expression-list> "."
<concatenate-statement> ::= "CONCATENATE" <expression-list> "INTO" <identifier> "."

<type>            ::= "string" | "i"
<expression>      ::= <identifier> | <integer> | <string>
<expression-list> ::= <expression-list> <expression> | <expression>

; NOTES:
; * Do strings have escaping mechanics?
; * Keywords restrict valid identifiers (otherwise we can't determinate the
;   last argument to CONCATENATE).
